apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cnpg-plugin-barman-cloud
  namespace: cnpg-system
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: cnpg-plugin-barman-cloud
    namespace: cnpg-system
  values:
    replicaCount: 1
    image:
      pullPolicy: Always
