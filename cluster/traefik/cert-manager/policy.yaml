---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: cert-manager
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: cert-manager
  policy:
    semver:
      range: '>=1.19.0 <2.0.0'