---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: redis-operator
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: redis-operator
  policy:
    semver:
      range: '>=0.23.0 <1.0.0'