apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: redis
  namespace: redis-system
spec:
  interval: 5m
  url: oci://registry-1.docker.io/bitnamicharts/redis
  ref:
    semver: 24.1.3 # {"$imagepolicy": "flux-system:redis:tag"}
