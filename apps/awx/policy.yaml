---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: awx-operator
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: awx-operator
  policy:
    semver:
      range: '>=2.19.0 <3.0.0'