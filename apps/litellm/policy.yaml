---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: litellm
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: litellm
  policy:
    semver:
      range: '>=1.83.0 <2.0.0'
