---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: woodpecker
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: woodpecker
  policy:
    semver:
      range: '>=3.6.4'