---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: outline
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: outline
  policy:
    semver:
      range: '>=1.3.0 <2.0.0'