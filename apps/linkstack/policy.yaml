---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: linkstack
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: linkstack
  policy:
    semver:
      range: '>=0.3.1'