when:
  - event: push
    branch: main

steps:
  check-environment:
    image: busybox
    commands:
      - echo "Woodpecker CI is working!"
      - echo "Current user is $(whoami)"
      - uname -a
      - pwd