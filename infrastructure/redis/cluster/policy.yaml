---
apiVersion: image.toolkit.fluxcd.io/v1
kind: ImagePolicy
metadata:
  name: redis-cluster
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: redis-cluster
  policy:
    semver:
      range: '>=0.17.0 <1.0.0'