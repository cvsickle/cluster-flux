apiVersion: source.toolkit.fluxcd.io/v1
kind: OCIRepository
metadata:
  name: redis
  namespace: redis-system
spec:
  interval: 5m
  url: oci://registry-1.docker.io/bitnamicharts/redis-cluster
  ref:
    semver: 13.0.4 # {"$imagepolicy": "flux-system:redis:tag"}
